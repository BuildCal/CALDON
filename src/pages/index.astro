---
import BaseLayout from '../layouts/BaseLayout.astro';
---

<BaseLayout title="Property Development NSW" description="CALDON™: Private property development across NSW. Serving Taree, Port Macquarie, Coffs Harbour, Newcastle, Sydney, Tamworth, Forster Tuncurry, and more.">
  <style>
    .hero {
      position: relative;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      height: 100vh;
      padding: 0 20px;
      text-align: center;
      overflow: hidden;
    }

    .hero-content {
      max-width: 800px;
      position: relative;
      z-index: 2;
    }

    .hero-logo {
      max-width: 180px;
      height: auto;
      margin-bottom: 40px;
      transition: transform 0.1s ease-out;
    }

    .hero-tagline {
      font-family: 'Cormorant Garamond', serif;
      font-weight: 300;
      font-size: 2rem;
      letter-spacing: 1px;
      color: var(--text-primary);
      margin-bottom: 20px;
      line-height: 1.4;
    }

    .hero-subtext {
      font-size: 1rem;
      color: var(--text-secondary);
      max-width: 600px;
      margin: 0 auto 50px;
      opacity: 0.9;
    }

    .cta-section {
      display: flex;
      flex-direction: column;
      align-items: center;
      gap: 30px;
      margin-top: 40px;
    }

    .primary-button {
      display: inline-block;
      padding: 15px 40px;
      background-color: var(--accent);
      color: white;
      text-decoration: none;
      font-size: 0.85rem;
      font-weight: 400;
      text-transform: uppercase;
      letter-spacing: 2px;
      transition: var(--transition);
      cursor: pointer;
      min-width: 250px;
    }
    
    .primary-button:hover {
      background-color: #333333;
      transform: translateY(-2px);
    }

    .scroll-indicator {
      position: absolute;
      bottom: 40px;
      left: 50%;
      transform: translateX(-50%);
      font-size: 11px;
      letter-spacing: 3px;
      text-transform: uppercase;
      color: var(--text-secondary);
      opacity: 0.7;
    }

    .scroll-indicator::after {
      content: '';
      display: block;
      width: 1px;
      height: 40px;
      background-color: var(--text-secondary);
      margin: 10px auto 0;
      opacity: 0.3;
    }

    /* Partnership Section */
    .partnership {
      padding: 80px 0;
      background-color: var(--subtle-accent);
      border-top: 1px solid var(--border);
    }

    .partnership-content {
      display: grid;
      grid-template-columns: 1fr auto;
      gap: 60px;
      align-items: center;
      max-width: 1000px;
      margin: 0 auto;
    }

    .partnership-text {
      max-width: 500px;
    }

    .partnership-subtitle {
      font-size: 0.75rem;
      font-weight: 400;
      letter-spacing: 3px;
      text-transform: uppercase;
      color: var(--text-secondary);
      margin-bottom: 10px;
    }

    .partnership-title {
      font-family: 'Cormorant Garamond', serif;
      font-size: 2rem;
      font-weight: 300;
      color: var(--text-primary);
      margin-bottom: 20px;
      line-height: 1.3;
    }

    .partnership-description {
      font-size: 0.95rem;
      color: var(--text-secondary);
      line-height: 1.7;
    }

    .partnership-description a {
      color: var(--text-primary);
      text-decoration: underline;
    }

    .partnership-logo {
      display: flex;
      justify-content: center;
      align-items: center;
    }

    .wattle-logo {
      max-width: 300px;
      height: auto;
      transition: var(--transition);
      opacity: 0.9;
    }

    .wattle-logo:hover {
      opacity: 1;
      transform: translateY(-2px);
    }

    .partnership-links {
      display: flex;
      gap: 30px;
      margin-top: 20px;
    }

    .partnership-read-more {
      display: inline-block;
      font-size: 0.8rem;
      color: var(--text-primary);
      text-transform: uppercase;
      letter-spacing: 2px;
      text-decoration: none;
      border-bottom: 1px solid var(--text-primary);
      padding-bottom: 2px;
      transition: var(--transition);
    }

    .partnership-read-more:hover {
      opacity: 0.7;
    }

    .partnership-read-more.external::after {
      content: '↗';
      font-size: 0.8em;
      margin-left: 5px;
    }

    /* Animations */
    @keyframes fadeIn {
      from { opacity: 0; }
      to { opacity: 1; }
    }

    @keyframes fadeUp {
      from { opacity: 0; transform: translateY(30px); }
      to { opacity: 1; transform: translateY(0); }
    }

    .fade-in {
      opacity: 0;
      animation: fadeIn 1.8s cubic-bezier(0.165, 0.84, 0.44, 1) forwards;
    }

    .fade-up {
      opacity: 0;
      animation: fadeUp 1.2s cubic-bezier(0.165, 0.84, 0.44, 1) forwards;
    }

    @media (max-width: 768px) {
      .hero-tagline {
        font-size: 1.8rem;
      }

      .hero-subtext {
        font-size: 0.9rem;
      }

      .primary-button {
        width: 100%;
        max-width: 300px;
      }

      .partnership-content {
        grid-template-columns: 1fr;
        gap: 40px;
        text-align: center;
      }

      .partnership-text {
        max-width: 100%;
      }

      .partnership-title {
        font-size: 1.8rem;
      }

      .wattle-logo {
        max-width: 250px;
      }

      .partnership-links {
        flex-direction: column;
        gap: 15px;
        align-items: center;
      }
    }

    @media (max-width: 576px) {
      .hero {
        height: 80vh;
        padding-top: 80px;
      }

      .hero-logo {
        max-width: 150px;
        margin-bottom: 30px;
      }

      .hero-tagline {
        font-size: 1.5rem;
      }

      .primary-button {
        padding: 12px 30px;
      }

      .partnership-title {
        font-size: 1.5rem;
      }

      .wattle-logo {
        max-width: 200px;
      }
    }
  </style>

  <div class="hero">
    <div class="hero-content">
      <img src="/logo.png" alt="CALDON™ Logo" class="hero-logo fade-in">
      <p class="hero-tagline fade-up">Property Development</p>
      <p class="hero-subtext fade-up" style="animation-delay: 0.3s;">Strategic property development and investment solutions across New South Wales.</p>
      
      <div class="cta-section fade-up" style="animation-delay: 0.5s;">
        <a href="/development" class="primary-button">
          Enter Site
        </a>
      </div>
    </div>
    <div class="scroll-indicator">Discover</div>
  </div>

  <!-- Partnership Section -->
  <section class="partnership">
    <div class="container">
      <div class="partnership-content">
        <div class="partnership-text">
          <p class="partnership-subtitle">Strategic Partnership</p>
          <h2 class="partnership-title">Building Excellence</h2>
          <p class="partnership-description">CALDON™ previously built homes under our own brand but has now partnered with Wattle Court to deliver quality construction as <a href="https://wattlecourt.com.au/builder/mid-coast/" target="_blank" rel="noopener noreferrer">Wattle Court Mid-Coast</a>. Our experienced local construction team continues building homes in Taree, Port Macquarie, Forster, Wauchope, Gloucester and surrounds.</p>
          <div class="partnership-links">
            <a href="/news#wattle-court-article" class="partnership-read-more">Read Our Coverage</a>
            <a href="https://www.myaustraliannews.com.au/news-releases.html?id=1032129&headline=wattle-court-plants-roots-in-nsw-mid-coast&pageNumber=7" target="_blank" rel="noopener noreferrer" class="partnership-read-more external">Read Full Article</a>
          </div>
        </div>
        <div class="partnership-logo">
          <a href="https://wattlecourt.com.au/builder/mid-coast/" target="_blank" rel="noopener noreferrer">
            <img src="/wattle.png" alt="Wattle Court - Homes Built for Life" class="wattle-logo">
          </a>
        </div>
      </div>
    </div>
  </section>

  <script>
    // Parallax effect on hero
    window.addEventListener('scroll', function() {
      const heroLogo = document.querySelector('.hero-logo');
      const heroTagline = document.querySelector('.hero-tagline');
      const heroSubtext = document.querySelector('.hero-subtext');
      const scrollPosition = window.scrollY;

      const maxScroll = window.innerHeight * 0.5;
      if (scrollPosition <= maxScroll && heroLogo && heroTagline && heroSubtext) {
        const translateFactor = scrollPosition * 0.2;
        heroLogo.style.transform = `translateY(${translateFactor}px)`;
        heroTagline.style.transform = `translateY(${translateFactor * 0.8}px)`;
        heroSubtext.style.opacity = String(Math.max(0.9 - scrollPosition / maxScroll, 0));
      }
    });
  </script>
</BaseLayout>
